<div class="container">
  
  <h2 class="mt-5 bg-light d-inline-block">配送先登録/一覧</h2>
  
  <%= form_with model:@address, url:shipping_addresses_path do |f| %>
    <table cellpadding="10">
      <tr><th>郵便番号(ハイフンなし)</th><td><%= f.text_field :postal_code, placeholder: '0000000' %></td></tr>
      <tr><th>住所</th><td><%= f.text_field :address, placeholder: '東京都渋谷区代々木神園町0-0' %></td></tr>
      <tr><th>宛名</th><td><%= f.text_field :address_name, placeholder: '令和道子' %></td></tr>
      <tr><th></th><td><%= f.submit "新規登録", class:"btn btn-success" %></td></tr>
    </table>
  <% end %>
  
  <table>
    <thead>
      <tr>
        <th>郵便番号</th>
        <th>住所</th>
        <th>宛名</th>
        <th></th>
      </tr>
    </thead>
    
    <tbody>
      <% @addresses.each do |shipping_address| %>
        <tr>
          <td><%= shipping_address.postal_code %></td>
          <td></td>
          <td></td>
          <td></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>