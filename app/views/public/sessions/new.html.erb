  <div class="container d-flex">
    <div class="mx-auto">
      
        <h2 class="mt-5 bg-light d-inline-block">ログイン</h2><br>

        <h3 class="mt-3 bg-light d-inline-block mb-5">会員の方はこちらからログイン</h3>

        
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <table cellpadding="10">
            <tr>
              <th>
                メールアドレス
              </th>
              <td>
                 <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
              </td>
            </tr>
            <tr>
              <th>
                パスワード
              </th>
              <td>
                <%= f.password_field :password, autocomplete: "current-password" %>
              </td>
            </tr>
            <tr>
              <th></th>
              <td>
                <%= f.submit "Log in", class:"btn btn-primary btn-md" %>
              </td>
            </tr>
          </table>
        <% end %>
        
        <h4 class="mt-5 bg-light d-inline-block">会員登録がお済みでない方</h4>
      　<h4><%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。</h4>
    </div>
  </div>